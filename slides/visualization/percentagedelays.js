d3.csv("average_delays_2015.csv", function(data) {

			console.log(data);
				
				
			.row(function(d){ return airline, percentage_delay:Number(d.percentage_delay.slice(1,2))})

			var height = 100;
  			var width = 100;

  			var delay = parseAll(percentage_delay);
  			console.log(delay);

  			var minDelay = d3.min(data, function(d){ return d.percentage_delay; });
  			var max = d3.max(data, function(d) { return d.column2; });

  			//console.log(minDelay);
  			//console.log(max);
		});